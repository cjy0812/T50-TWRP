【背景信息】

用户正在使用 GitHub Actions 临时服务器编译 Android TWRP Recovery，目标设备为 T50 平板。

当前项目：

仓库：T50-TWRP

设备树：

URL：https://github.com/cjy0812/T50-TWRP_device_tree

路径：device/zyb/tb8786p1_64_k510_wifi


设备：

tb8786p1_64_k510_wifi


平台：

MT6768


使用：

TWRP 12.1 manifest

Android recovery-as-boot 方案



【讨论主题】

主要讨论：

1. 修复 GitHub Actions TWRP 构建 workflow


2. 增加可选 SSH 调试功能


3. 根据本地成功复现的构建流程修改 CI


4. 分析 TWRP 编译失败原因


## 第二轮 更详细

【背景信息】

用户正在使用 GitHub Actions 临时 runner 编译 Android TWRP Recovery，目标设备为 T50 平板。

项目：

仓库：T50-TWRP

工作目录：

~/work/T50-TWRP/T50-TWRP/workspace

设备：

tb8786p1_64_k510_wifi

SoC：

MT6768

构建体系：

TWRP 12.1 manifest

Android 12.1 基础

recovery-as-boot 结构

A/B 设备，无独立 recovery 分区



【讨论主题】

本次主要围绕：

1. GitHub Actions 自动构建 TWRP workflow 修复


2. 本地复现成功后的 CI 流程迁移


3. SSH 调试入口改为用户可选


4. TWRP 编译阶段分析


5. boot.img 生成失败原因定位


6. 当前 make/ninja 日志详细程度判断



【重要信息】

一、设备树信息

设备树：

https://github.com/cjy0812/T50-TWRP_device_tree

路径：

device/zyb/tb8786p1_64_k510_wifi

BoardConfig.mk：

TARGET_NO_BOOTLOADER := true

TARGET_SCREEN_DENSITY := 320


# Kernel

BOARD_BOOTIMG_HEADER_VERSION := 4

BOARD_KERNEL_BASE := 0x40078000

BOARD_KERNEL_CMDLINE := bootopt=64S3,32N2,64N2 buildvariant=user

BOARD_KERNEL_PAGESIZE := 4096

BOARD_RAMDISK_OFFSET := 0x07c08000

BOARD_KERNEL_TAGS_OFFSET := 0x0bc08000

BOARD_KERNEL_IMAGE_NAME := Image

BOARD_INCLUDE_DTB_IN_BOOTIMG := true


TARGET_KERNEL_CONFIG := tb8786p1_64_k510_wifi_defconfig

TARGET_KERNEL_SOURCE := kernel/zyb/tb8786p1_64_k510_wifi


# 使用预编译 kernel

TARGET_FORCE_PREBUILT_KERNEL := true

TARGET_PREBUILT_KERNEL := $(DEVICE_PATH)/prebuilt/kernel

TARGET_PREBUILT_DTB := $(DEVICE_PATH)/prebuilt/dtb.img


BOARD_MKBOOTIMG_ARGS += --dtb $(TARGET_PREBUILT_DTB)

BOARD_INCLUDE_DTB_IN_BOOTIMG :=


# 分区

BOARD_FLASH_BLOCK_SIZE := 262144

BOARD_BOOTIMAGE_PARTITION_SIZE := 67108864

BOARD_RECOVERYIMAGE_PARTITION_SIZE := 67108864


# 平台

TARGET_BOARD_PLATFORM := mt6768


# AVB

BOARD_AVB_ENABLE := true

BOARD_AVB_MAKE_VBMETA_IMAGE_ARGS += --flags 3

二、kernel/dtb情况

设备树预编译文件：

目录：

device/zyb/tb8786p1_64_k510_wifi/prebuilt/

文件：

dtb.img   156K
kernel    82M

确认构建系统确实使用预编译 kernel：

执行：

grep -n "TARGET_PREBUILT_KERNEL\|BOARD_PREBUILT_KERNEL\|TARGET_KERNEL_SOURCE\|TARGET_KERNEL_CONFIG" device/zyb/tb8786p1_64_k510_wifi/BoardConfig.mk

结果：

TARGET_KERNEL_CONFIG := tb8786p1_64_k510_wifi_defconfig
TARGET_KERNEL_SOURCE := kernel/zyb/tb8786p1_64_k510_wifi
TARGET_PREBUILT_KERNEL := $(DEVICE_PATH)/prebuilt/kernel

ninja 中确认：

grep -n "prebuilt/kernel" out/build-twrp_tb8786p1_64_k510_wifi.ninja

输出：

cp "device/zyb/tb8786p1_64_k510_wifi/prebuilt/kernel"
"out/target/product/tb8786p1_64_k510_wifi/kernel"

说明：

kernel 没有重新编译

构建直接复制 prebuilt/kernel


三、关键 build var

执行：

get_build_var BOARD_USES_RECOVERY_AS_BOOT

输出：

true

其他：

get_build_var BOARD_BOOTIMAGE_PARTITION_SIZE

输出：

67108864

get_build_var BOARD_KERNEL_IMAGE_NAME

输出：

Image

get_build_var BOARD_INCLUDE_DTB_IN_BOOTIMG

没有输出。

四、构建流程

workflow 当前：

初始化：

repo init
repo sync

设备树：

git clone DEVICE_TREE_URL DEVICE_PATH

构建：

source build/envsetup.sh

export ALLOW_MISSING_DEPENDENCIES=true

lunch twrp_tb8786p1_64_k510_wifi-eng

make recoveryimage -j$(nproc --all)

当前 workflow 已修改：

BUILD_TARGET 从：

dist

改为：

recoveryimage


原因： 之前目标不明确，现在直接构建 recovery。

五、GitHub Actions 修改内容

新增：

ENABLE_SSH:
  description: '是否开启 SSH 调试'
  required: true
  default: 'false'

SSH：

- name: Debug SSH
  if: ${{ github.event.inputs.ENABLE_SSH == 'true' }}
  uses: mxschmitt/action-tmate@v3

效果：

默认不开 SSH

用户手动 workflow_dispatch 时选择开启


六、编译结果

完整编译已经到：

[100% 20957/20957] Target boot image from recovery

说明：

已经完成：

Android framework 编译

bionic

vold

recovery ramdisk

ninja 全部目标


失败位置：

out/target/product/tb8786p1_64_k510_wifi/boot.img

完整错误：

FAILED: out/target/product/tb8786p1_64_k510_wifi/boot.img


error: boot.img too large

(105869312 > 67039232)

实际：

生成：

105869312 bytes

限制：

67108864 bytes

可用最大：

67039232 bytes

超出：

约：

38MB+

失败命令：

mkbootimg \
 --kernel out/target/product/tb8786p1_64_k510_wifi/kernel \
 --ramdisk out/target/product/tb8786p1_64_k510_wifi/ramdisk-recovery.img \
 --header_version 4 \
 --dtb device/zyb/tb8786p1_64_k510_wifi/prebuilt/dtb.img \
 --output boot.img


avbtool add_hash_footer ...

说明： 失败发生在：

mkbootimg生成boot.img
+
avb footer处理前检查大小

不是源码编译失败。

七、make 日志情况

当前：

make recoveryimage -j$(nproc --all)

日志：

属于 ninja 默认模式：

例如：

[31% 6529/20957] //bionic/libc clang xxx
[100% 20957/20957] Target boot image from recovery
FAILED:

可以看到：

当前任务

百分比

错误位置

失败命令


没有开启：

showcommands

或者：

-v

如果需要完整编译命令可以增加：

export NINJA_ARGS="-v"

但当前问题不需要。

【已经确认的结论】

1. GitHub Actions workflow 已基本修复：

SSH 已变为可选

构建目标调整正确

构建流程符合本地复现流程

输出检查加入



2. CI 环境不是当前问题：

repo sync 正常

依赖正常

编译正常

ninja 完成 100%



3. 当前问题是 TWRP 镜像布局/大小问题：



boot.img 超过 boot 分区限制

4. 当前设备采用：



BOARD_USES_RECOVERY_AS_BOOT=true

所以：

recovery 内容最终进入 boot.img

没有单独 recovery 分区


5. 当前不建议继续修改 CI。 应该优先分析镜像大小来源。



【未解决问题】

1. boot.img 为什么达到 105MB

需要继续查看：

ls -lh out/target/product/tb8786p1_64_k510_wifi/kernel

ls -lh out/target/product/tb8786p1_64_k510_wifi/ramdisk-recovery.img

判断：

kernel 82MB 是否主要原因

ramdisk 是否异常大


2. 是否需要重新处理 kernel

目前：

prebuilt/kernel = 82M

可能导致：

boot 分区无法容纳。

需要确认：

原厂 boot.img kernel 大小

压缩方式

Image.gz / Image.gz-dtb 区别


3. 是否需要修改 boot image 方案

可能涉及：

recovery-as-boot

vendor_boot

header version 4

MTK boot image布局


4. 实际分区大小是否正确

当前：

BOARD_BOOTIMAGE_PARTITION_SIZE=67108864

需要确认是否来自真实 GPT。

【后续继续对话时需要知道】

当前不要重新检查 YAML，workflow 已经可以跑。

当前阶段重点是 TWRP boot.img 超尺寸。

不要先开 verbose 编译。

下一步应该收集：


ls -lh out/target/product/tb8786p1_64_k510_wifi/kernel

ls -lh out/target/product/tb8786p1_64_k510_wifi/ramdisk-recovery.img

ls -lh out/target/product/tb8786p1_64_k510_wifi/boot.img

以及必要时：

unpackbootimg

分析原厂 boot.img 与 TWRP boot.img 差异。

当前状态：

CI 已成功进入 TWRP 镜像生成阶段，剩余问题属于设备适配问题，不是构建环境问题。
5. 判断当前 make 日志详细程度



【重要信息】

设备树 BoardConfig.mk 关键配置：

BOARD_BOOTIMG_HEADER_VERSION := 4
BOARD_KERNEL_BASE := 0x40078000
BOARD_KERNEL_CMDLINE := bootopt=64S3,32N2,64N2 buildvariant=user
BOARD_KERNEL_PAGESIZE := 4096

BOARD_KERNEL_IMAGE_NAME := Image
BOARD_INCLUDE_DTB_IN_BOOTIMG := true

TARGET_KERNEL_CONFIG := tb8786p1_64_k510_wifi_defconfig
TARGET_KERNEL_SOURCE := kernel/zyb/tb8786p1_64_k510_wifi

TARGET_FORCE_PREBUILT_KERNEL := true
TARGET_PREBUILT_KERNEL := $(DEVICE_PATH)/prebuilt/kernel
TARGET_PREBUILT_DTB := $(DEVICE_PATH)/prebuilt/dtb.img

BOARD_BOOTIMAGE_PARTITION_SIZE := 67108864
BOARD_RECOVERYIMAGE_PARTITION_SIZE := 67108864

预编译 kernel：

device/zyb/tb8786p1_64_k510_wifi/prebuilt/

dtb.img   156K
kernel    82M

确认：

get_build_var BOARD_USES_RECOVERY_AS_BOOT

输出：

true

构建过程：

之前完整编译成功走到了：

[100% 20957/20957] Target boot image from recovery

失败原因：

FAILED: out/target/product/tb8786p1_64_k510_wifi/boot.img

error: boot.img too large
(105869312 > 67039232)

即：

生成 boot.img：

105869312 bytes


boot 分区限制：

67108864 bytes



超出约 40MB。

当前 workflow：

使用：

make recoveryimage -j$(nproc --all)

使用：

lunch twrp_tb8786p1_64_k510_wifi-eng

输出上传：


out/target/product/tb8786p1_64_k510_wifi/*.img
out/target/product/tb8786p1_64_k510_wifi/*.zip

SSH 调试：

新增 workflow input：

ENABLE_SSH:
  description: '是否开启 SSH 调试'
  required: true
  default: 'false'

使用：

- name: Debug SSH
  if: ${{ github.event.inputs.ENABLE_SSH == 'true' }}
  uses: mxschmitt/action-tmate@v3

make 日志：

当前：

make recoveryimage -j$(nproc --all)

属于 ninja 默认输出：

可以看到：

编译进度

当前目标

失败命令


不是完整 verbose。

【已经确认的结论】

1. 当前 GitHub Actions workflow 已经基本修复：

SSH 改为用户控制

构建目标改为 recoveryimage

输出检查修复

构建流程与本地成功复现一致



2. 当前 CI 已经能进入真实 TWRP 镜像生成阶段。


3. 现在问题不是 CI：

不是 repo

不是环境

不是依赖


而是 boot.img 大小超出分区限制。


4. 当前 make 默认日志足够定位问题，不需要立即开启 verbose。


5. 下一步重点应该分析：

boot.img 为什么 105MB

kernel/dtb/ramdisk 哪部分占空间

recovery-as-boot 配置是否正确




【未解决问题】

1. 如何解决：



boot.img too large
105869312 > 67108864

需要继续分析：

kernel 是否过大

ramdisk-recovery.img 是否过大

是否应该调整 boot image 结构

是否需要 vendor_boot/recovery 方案


2. 是否需要修改：



BOARD_BOOTIMAGE_PARTITION_SIZE := 67108864

或者重新适配设备实际分区。

3. 是否需要进一步获取：



ls -lh out/target/product/tb8786p1_64_k510_wifi/kernel
ls -lh out/target/product/tb8786p1_64_k510_wifi/ramdisk-recovery.img

分析镜像组成。

【后续继续对话时需要知道】

不需要重新检查 GitHub Actions YAML，当前版本已经可以运行。

当前阶段不要继续改 CI，先解决 TWRP boot.img 超尺寸问题。

设备是 A/B + recovery-as-boot：

没有独立 recovery 分区。

TWRP 最终进入 boot.img。


kernel 使用 prebuilt：

device/zyb/tb8786p1_64_k510_wifi/prebuilt/kernel

当前失败发生在：


mkbootimg -> avbtool add_hash_footer

之前已经完成 kernel、ramdisk、dtb 组合阶段。
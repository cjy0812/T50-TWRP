# T50 TWRP 构建背景信息

本文是当前 T50/ZPD1321 TWRP 适配的整理版背景。旧讨论中有多轮试错结论，后续判断以这里和 `docs/raw-dump-partitions.md` 为准。

## 当前项目

- Builder 仓库：`cjy0812/T50-TWRP`
- 设备树仓库：`cjy0812/T50-TWRP_device_tree`
- 设备树路径：`device/zyb/tb8786p1_64_k510_wifi`
- 目标设备：T50 / tb8786p1_64_k510_wifi
- 设备代号：`tb8786p1_64_k510_wifi`
- 平台：MediaTek MT6768 family
- 构建基线：TWRP 12.1 minimal manifest
- 学习系统转储：`G:\T50\Raw_dump`
- 当前工厂底座：`G:\T50\T50-aosp-zyyme-260716`

## 已确认事实

- `Raw_dump` 和工厂包里都没有独立 `recovery` 分区。
- `boot_a.bin` 是 32 MiB，包含 kernel 和小型 generic boot ramdisk。
- `vendor_boot_a.bin` 是 64 MiB，header v4，包含 vendor ramdisk、DTB、first-stage fstab、recovery rc 和大量内核模块。
- 当前设备 fastbootd fetch 出来的 `vendor_boot_a` 与 `G:\T50\T50-aosp-zyyme-260716\vendor_boot_a.bin` 哈希一致，因此当前适配底座以工厂包为准。
- 因为 recovery 资源位于 `vendor_boot` 方案中，TWRP 主构建目标应为 `vendorbootimage`。
- `recoveryimage` 不适合 T50；它会生成一个设备上不存在的 `recovery.img`，并受到 32 MiB 限制。
- 工厂包 `super.bin` 中可见 Android 14 / SDK 34 userdebug/test-keys 信息：

```properties
ro.product.build.fingerprint=alps/sys_mssi_t_64_cn_wifi/mssi_t_64_cn_wifi:14/UP1A.231005.007/mp1V814:userdebug/test-keys
ro.product.build.version.release=14
ro.product.build.version.sdk=34
ro.product.ab_ota_partitions=boot,product,system,system_ext,vendor
```

## 关键配置方向

设备树需要使用 Android build 真正识别的变量名：

```makefile
BOARD_BOOT_HEADER_VERSION := 4
BOARD_VENDOR_BOOTIMAGE_PARTITION_SIZE := 67108864
BOARD_MOVE_RECOVERY_RESOURCES_TO_VENDOR_BOOT := true
BOARD_INCLUDE_RECOVERY_RAMDISK_IN_VENDOR_BOOT := true
PRODUCT_BUILD_VENDOR_BOOT_IMAGE := true
```

不要再使用这些容易误导的生成器变量名：

```makefile
BOARD_BOOTIMG_HEADER_VERSION
BOARD_VENDORBOOTIMAGE_PARTITION_SIZE
```

## 当前构建目标

GitHub Actions workflow 默认参数：

```text
MANIFEST_URL=https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp
MANIFEST_BRANCH=twrp-12.1
DEVICE_TREE_URL=https://github.com/cjy0812/T50-TWRP_device_tree
DEVICE_TREE_BRANCH=twrp-12.1
DEVICE_PATH=device/zyb/tb8786p1_64_k510_wifi
DEVICE_NAME=tb8786p1_64_k510_wifi
MAKEFILE_NAME=twrp_tb8786p1_64_k510_wifi
BUILD_TARGET=vendorbootimage
```

构建命令核心：

```bash
source build/envsetup.sh
export ALLOW_MISSING_DEPENDENCIES=true
lunch twrp_tb8786p1_64_k510_wifi-eng
make vendorbootimage -j$(nproc --all)
```

## 已踩过的坑

- 把设备误判成有独立 `recovery` 分区，导致 `recovery.img too large`。
- 把设备误判成普通 recovery-as-boot，导致 `boot.img` 超过 32 MiB。
- 使用生成器产出的非标准变量名，导致 `vendorbootimage` 没有真正生成目标。
- 旧文档记录过 Android 12 / SP1A 信息，但 `super.bin` 中的新证据指向 Android 14 / UP1A。
- 混淆学习系统 `Raw_dump` 和工厂包底座；当前 fastbootd 设备实际匹配工厂包。

## 下一步验证

1. 推送设备树修正后触发 CI。
2. 确认日志中这些变量有值：

```bash
get_build_var BOARD_BOOT_HEADER_VERSION
get_build_var BOARD_VENDOR_BOOTIMAGE_PARTITION_SIZE
get_build_var BOARD_MOVE_RECOVERY_RESOURCES_TO_VENDOR_BOOT
get_build_var BOARD_INCLUDE_RECOVERY_RAMDISK_IN_VENDOR_BOOT
get_build_var INSTALLED_VENDOR_BOOTIMAGE_TARGET
```

3. 成功产物应包含：

```text
out/target/product/tb8786p1_64_k510_wifi/vendor_boot.img
```

4. Release 里除了 `vendor_boot.img`，还应上传 `recovery.cpio.gz`，用于后续离线 repack 或 fastbootd fragment 实验。
